 ) : int == 2 ? (
            <ShowBackgroundCheck
              int={2}
              open={open}
              setOpen={setOpen}
              data={location?.state}
              showTableCount={setCount}
              setShowModal={setShowModal}
              showModal={undefined} //   setShowModal={showModal}
            />
